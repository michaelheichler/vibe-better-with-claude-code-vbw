---
phase: 2
plan: 4
title: Align docs with the consolidated resolver and close out the phase
type: execute
wave: 3
depends_on: [3]
cross_phase_deps: []
autonomous: true
skills_used: []
files_modified:
  - docs/wiki/plugin-root-resolution.md
  - CLAUDE.md
  - AGENTS.md
  - scripts/bash-guard.sh
  - tests/qa-bash-guard.bats
  # Scope amendment: These files were added mid-execution as a required coordination fix discovered while running plan 02-04. bash-guard.sh's detect_agent_role lacked the payload-based agent_id/agent_type check that file-guard.sh already had, so it fell back to shared session-scoped active-agent markers that do not distinguish concurrent subagents. A concurrently running QA subagent's marker misclassified the 02-04 Docs agent as read-only "qa" and blocked it from committing its own work. Commit 6ae202bf fixed the root cause, was independently QA-verified, and is already merged.
  # Reconfirmed round 04: DEV-01 remains resolved via this plan-amendment, no further code or scope change needed.
must_haves:
  truths:
    - "docs/wiki/plugin-root-resolution.md names scripts/resolve-plugin-root.sh as the single command-cascade source of truth and no longer describes per-command inline cascades as current"
    - "CLAUDE.md's Plugin root resolution section describes the trampoline-plus-helper command cascade while keeping the hook cascade (DXP-01) description unchanged in policy"
    - "Docs describe the SessionStart session-link bootstrap and record the T-301 spike outcome"
  artifacts:
    - {path: "docs/wiki/plugin-root-resolution.md", provides: "updated resolution wiki", contains: "resolve-plugin-root.sh"}
    - {path: "CLAUDE.md", provides: "updated Key Patterns section", contains: "resolve-plugin-root.sh"}
  key_links:
    - {from: "docs/wiki/plugin-root-resolution.md", to: "scripts/resolve-plugin-root.sh", via: "documents the helper as canonical command cascade"}
---
<objective>
Update the two documentation surfaces that assert the old duplicated-cascade architecture so they describe the consolidated helper, preserving the documented hook-versus-command policy distinction, then run the full suite as phase close-out. Docs-only scope: no script or command changes here.
</objective>
<context>
@.vbw-planning/phases/02-structural-resolution-consolidation/02-RESEARCH.md
@docs/wiki/plugin-root-resolution.md
Rationale: RESEARCH.md "Required supporting updates" limits doc changes to describing the new command-helper source of truth and the preserved policy split. Wiki currently claims execute-protocol.md is canonical, which plan 3 changed (REQ-01, T-302, C1).
</context>
<tasks>
<task type="auto">
  <name>Update docs/wiki/plugin-root-resolution.md</name>
  <files>
    docs/wiki/plugin-root-resolution.md
  </files>
  <action>
Rewrite the command-cascade sections to describe scripts/resolve-plugin-root.sh as the single implementation (nine-step order, --require-script and --nonfatal modes, session-link trampoline, SessionStart bootstrap), correct the stale claim that references/execute-protocol.md carries a canonical inline copy, keep the hook-cascade section and the exemption list of seven non-preamble commands accurate, and record the T-301 spike verdict from 02-SPIKE-T301.md where the wiki discusses variable propagation.
  </action>
  <verify>
grep shows resolve-plugin-root.sh referenced and no remaining prose claiming each command embeds the cascade inline.
  </verify>
  <done>
Wiki matches the shipped architecture, including bootstrap and mode documentation.
  </done>
</task>
<task type="auto">
  <name>Update the CLAUDE.md Key Patterns section</name>
  <files>
    CLAUDE.md
    AGENTS.md
  </files>
  <action>
Update the "Plugin root resolution" Key Patterns entry: the command cascade is now implemented once in scripts/resolve-plugin-root.sh and invoked via a session-link trampoline in the 19 target commands. Keep the hook-cascade (DXP-01) description and its priority-order rationale unchanged apart from wording that referenced inline duplication. Do not touch unrelated CLAUDE.md sections (that is Phase 7 territory).

Scope note (added during execution, orchestrator-approved): CLAUDE.md is a symlink to AGENTS.md in this repo. Edit the real file, AGENTS.md, directly (the Edit tool refuses to write through the symlink path). CLAUDE.md itself needs no separate edit since it resolves to the same content.
  </action>
  <verify>
AGENTS.md diff is confined to the Plugin root resolution subsection. The hook-cascade policy text is semantically unchanged. `readlink CLAUDE.md` still resolves to AGENTS.md.
  </verify>
  <done>
AGENTS.md (read via the CLAUDE.md symlink) accurately describes both cascades post-consolidation.
  </done>
</task>
<task type="auto">
  <name>Phase close-out verification run</name>
  <files>
    .vbw-planning/phases/02-structural-resolution-consolidation/02-04-CLOSEOUT.md
  </files>
  <action>
Run bash testing/run-all.sh directly (no tail/tee wrappers) and confirm green. Confirm the phase success criteria with concrete evidence: (a) the cascade is defined once (grep counts across commands/ and references/ show zero inline cascade fragments and one helper implementation), (b) marketplace-root resolution evidence exists in 02-03-SMOKE.md or is re-run here if it was flagged as a gap. Record command outputs and counts in 02-04-CLOSEOUT.md. Any newly discovered failing tests follow the zero-tolerance policy: investigate and fix or report as blocker, never dismiss.
  </action>
  <verify>
02-04-CLOSEOUT.md contains the run-all result and the grep-count evidence for both success criteria.
  </verify>
  <done>
Suite green and both phase success criteria evidenced.
  </done>
</task>
</tasks>
<verification>
1. `grep -n 'resolve-plugin-root.sh' docs/wiki/plugin-root-resolution.md CLAUDE.md` non-empty
2. `bash testing/run-all.sh` green
3. 02-04-CLOSEOUT.md records evidence for both phase success criteria
</verification>
<success_criteria>
- Documentation describes the single-definition command cascade and preserved hook policy
- Phase success criteria (cascade defined once, marketplace-root still resolves) evidenced in close-out
</success_criteria>
<output>
02-04-SUMMARY.md
</output>
