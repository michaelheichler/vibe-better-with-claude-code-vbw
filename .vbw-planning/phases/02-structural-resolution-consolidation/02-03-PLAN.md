---
phase: 2
plan: 3
title: Replace every inline cascade with the shared resolver and rewrite the contract test
type: execute
wave: 2
depends_on: [1, 2]
cross_phase_deps: []
autonomous: true
skills_used: [running-claude-smoke-tests]
files_modified:
  - commands/config.md
  - commands/debug.md
  - commands/doctor.md
  - commands/fix.md
  - commands/help.md
  - commands/init.md
  - commands/map.md
  - commands/report.md
  - commands/research.md
  - commands/rtk.md
  - commands/skills.md
  - commands/update.md
  - commands/whats-new.md
  - commands/discuss.md
  - commands/qa.md
  - commands/resume.md
  - commands/status.md
  - commands/verify.md
  - commands/vibe.md
  - references/execute-protocol.md
  - testing/verify-plugin-root-resolution.sh
  - tests/auto-uat.bats
  - tests/template-nesting.bats
must_haves:
  truths:
    - "No command file contains an inline cascade fragment (marketplace scan, numeric-cache sort, ps axww recovery) outside a call to resolve-plugin-root.sh"
    - "All 19 preamble sites and all 8 nested/partial resolver copies remaining at execution time (7 phase-detect refresh blocks plus rtk.md's nonfatal resolver) route through scripts/resolve-plugin-root.sh"
    - "rtk.md still emits its status_unavailable JSON on resolution failure, via the helper's --nonfatal mode"
    - "The five phase-detect preambles and vibe.md retain their phase-detect cache write, locking, and error-output contracts outside the helper"
    - "testing/verify-plugin-root-resolution.sh tests the new helper behavior and trampoline contract, and prohibits legacy inline cascade fragments, so run-all.sh is green after the replacement (no test inversion)"
  artifacts:
    - {path: "commands/help.md", provides: "representative standard trampoline", contains: "resolve-plugin-root.sh"}
    - {path: "commands/rtk.md", provides: "nonfatal status resolution", contains: "--nonfatal"}
    - {path: "references/execute-protocol.md", provides: "single shared-helper invocation replacing the divergent partial copy", contains: "resolve-plugin-root.sh"}
    - {path: "testing/verify-plugin-root-resolution.sh", provides: "behavioral helper tests plus trampoline contract", contains: "resolve-plugin-root.sh"}
  key_links:
    - {from: "commands/*.md", to: "scripts/resolve-plugin-root.sh", via: "trampoline through the deterministic session link created at SessionStart"}
    - {from: "testing/verify-plugin-root-resolution.sh", to: "scripts/resolve-plugin-root.sh", via: "asserts helper behavior and forbids legacy fragments"}
---
<objective>
Root-cause consolidation: replace all 19 full command preambles, the 7 remaining nested phase-detect refresh cascades, rtk.md's second nonfatal resolver, and the divergent execute-protocol.md copy with calls to scripts/resolve-plugin-root.sh, and rewrite testing/verify-plugin-root-resolution.sh in the same plan so the suite asserts the new shape instead of failing red on the old one. Adding the helper alongside the old duplication is not acceptable.
</objective>
<context>
@.vbw-planning/phases/02-structural-resolution-consolidation/02-RESEARCH.md
@scripts/resolve-plugin-root.sh
@testing/verify-plugin-root-resolution.sh
@docs/wiki/plugin-root-resolution.md
@.claude/skills/running-claude-smoke-tests/SKILL.md
Rationale: RESEARCH.md inventories every duplication site with exact file:line locations and flags the test-inversion risk. The replacement and the contract-test rewrite must land in one plan or run-all.sh shows false-red between plans (REQ-01, T-302, C1). Depends on plan 1 (helper plus SessionStart link exist) and plan 2 (vibe.md/verify.md lazy-context edits land first, avoiding cross-plan merge conflicts on those files).
</context>
<tasks>
<task type="auto">
  <name>Replace the 13 standard preambles with the trampoline</name>
  <files>
    commands/config.md, commands/debug.md, commands/doctor.md, commands/fix.md, commands/help.md, commands/init.md, commands/map.md, commands/report.md, commands/research.md, commands/rtk.md, commands/skills.md, commands/update.md, commands/whats-new.md
  </files>
  <action>
Replace the exact 2,150-character preamble in each of the 13 standard commands (locations in RESEARCH.md) with a short trampoline expansion that resolves the root through the deterministic session link /tmp/.vbw-plugin-root-link-${CLAUDE_SESSION_ID:-default} (created at SessionStart by plan 1) and invokes scripts/resolve-plugin-root.sh, keeping the existing exit-1-with-diagnostic failure semantics and the exported variables downstream template blocks consume. The trampoline needs one minimal guarded fallback for a missing link (invoke the helper via CLAUDE_PLUGIN_ROOT if set, else fail with the diagnostic). Do not reproduce cascade steps inline. For rtk.md this task covers only its main preamble. Its nonfatal status resolver is task 3.
  </action>
  <verify>
grep for legacy fragments (plugins/marketplaces, ps axww, sort -V cache globs) in these 13 files returns only the helper invocation lines, if anything. Each command still sets the same downstream variables.
  </verify>
  <done>
13 preambles replaced with the trampoline. No inline cascade steps remain in these files.
  </done>
</task>
<task type="auto">
  <name>Replace the 5 phase-detect preambles and vibe.md's specialized preamble</name>
  <files>
    commands/discuss.md, commands/qa.md, commands/resume.md, commands/status.md, commands/verify.md, commands/vibe.md
  </files>
  <action>
Replace the 2,379-character preambles in discuss/qa/resume/status/verify and the 2,831-character vibe.md preamble with the same trampoline from task 1, followed by only their command-specific logic: the phase-detect execution and cache write for the five, and additionally the cache locking and retry behavior for vibe.md. Extract only the common resolver. The phase-detect invocation, locking, and error-output contracts stay in the command files. Coordinate with plan 2's already-landed edits in vibe.md and verify.md (routing-deferred blocks). Do not move them back.
  </action>
  <verify>
Each of the six files contains the trampoline plus its phase-detect logic and no inline cascade. Phase-detect cache behavior is unchanged when the helper resolves successfully.
  </verify>
  <done>
Six preambles delegated to the helper with phase-detect and locking behavior preserved.
  </done>
</task>
<task type="auto">
  <name>Replace the 8 remaining nested/partial resolvers and execute-protocol.md</name>
  <files>
    commands/discuss.md, commands/qa.md, commands/resume.md, commands/status.md, commands/verify.md, commands/vibe.md, commands/rtk.md, references/execute-protocol.md
  </files>
  <action>
Replace the 7 remaining nested _refresh_phase_detect_link cascades (discuss.md:33, qa.md:45, resume.md:31, status.md:48, verify.md:46/:110, vibe.md:46) with an invocation of resolve-plugin-root.sh --require-script phase-detect.sh, which also fixes their missing marketplace-root branch. Amendment (round-02 remediation, DEV-02): the original plan inventoried 10 sites from RESEARCH.md, but plan 02-02 (commits 2e5099f8, 91f8cdb5, 7475fd0d) removed the verify.md:182, vibe.md:148, and vibe.md:254 copies before this plan executed, leaving 7 sites. Execution correctly consolidated those 7. This note records the corrected inventory. Replace rtk.md's 2,196-character nonfatal resolver (rtk.md:21) with resolve-plugin-root.sh --nonfatal, preserving the status_unavailable JSON object on failure. Replace the divergent resolver in references/execute-protocol.md:4-55 with a single shared-helper invocation, which also fixes its absent marketplace-root scan and missing session-link creation.
  </action>
  <verify>
grep -c 'VBW_CACHE_ROOT=' across commands/ and references/ drops from 30-plus to the trampoline-only count. rtk.md failure path still emits status_unavailable JSON (manually simulate by pointing the helper at an empty fixture).
  </verify>
  <done>
All nested, nonfatal, and reference-doc resolver copies route through the helper. Zero divergent cascade orderings remain.
  </done>
</task>
<task type="auto">
  <name>Rewrite testing/verify-plugin-root-resolution.sh for the new shape</name>
  <files>
    testing/verify-plugin-root-resolution.sh
    tests/auto-uat.bats
    tests/template-nesting.bats
  </files>
  <action>
Rewrite the contract test (currently asserting inline cascade fragments at :269-429): (a) behavioral tests exercising the helper's precedence order, marketplace-root fallback, exit codes, canonicalization, and link repair against fixture directory shapes; (b) a static contract over the TARGET_COMMANDS list requiring each target command to invoke resolve-plugin-root.sh and PROHIBITING legacy inline fragments (plugins/marketplaces scan, ps axww recovery, numeric-cache sort) outside the helper; (c) keep the existing seven-command exemption list (compress, list-todos, pause, profile, teach, todo, uninstall). Do not duplicate tests/resolve-plugin-root.bats coverage. This script owns the command-file contract, the bats file owns helper behavior.

Scope amendment (added during execution, orchestrator-approved): tasks 1-3's replacement of inline cascades/nested refreshes with the shared helper broke fixtures in tests/auto-uat.bats and tests/template-nesting.bats that were built around the old (buggier) inline cascade behavior. Update those fixtures/assertions to match the new resolve-plugin-root.sh-based architecture rather than reverting the correctness fix. This is in-scope completion of the "no test inversion" requirement below, not a separate bug.
  </action>
  <verify>
bash testing/verify-plugin-root-resolution.sh passes against the replaced commands and fails if a legacy fragment is reintroduced into a command file (spot-check by temporarily inserting one). tests/auto-uat.bats and tests/template-nesting.bats pass against the new architecture.
  </verify>
  <done>
Contract test asserts the new architecture. bash testing/run-all.sh is fully green.
  </done>
</task>
<task type="auto">
  <name>Fresh-session smoke check across both install shapes</name>
  <files>
    .vbw-planning/phases/02-structural-resolution-consolidation/02-03-SMOKE.md
  </files>
  <action>
Run a real fresh-session smoke test in a --plugin-dir install and a marketplace-root "source":"./" install (per the running-claude-smoke-tests approach used in this repo): invoke a standard command (/vbw:help) and a phase-detect command (/vbw:status) as the first commands of a fresh session and confirm root resolution succeeds through the SessionStart link with no inline cascade. Record results in 02-03-SMOKE.md. If the marketplace-root shape cannot be exercised from this environment, record exactly what was run and flag the gap for QA rather than claiming success.
  </action>
  <verify>
02-03-SMOKE.md records per-shape outcomes with the session evidence paths.
  </verify>
  <done>
Marketplace-root install demonstrably still resolves (phase success criterion) or the gap is explicitly flagged for verification.
  </done>
</task>
</tasks>
<verification>
1. `grep -rln 'plugins/marketplaces' commands/ references/` returns nothing outside helper-invocation trampolines
2. `grep -rc 'ps axww' commands/` totals 0
3. `bash testing/verify-plugin-root-resolution.sh` and `bash testing/run-all.sh` green
4. 02-03-SMOKE.md shows marketplace-root resolution evidence or an explicit flagged gap
</verification>
<success_criteria>
- Cascade defined once: all 19 preambles and 8 remaining nested/partial copies call scripts/resolve-plugin-root.sh
- Marketplace-root install still resolves (smoke evidence)
- run-all.sh green with the rewritten contract test (no test inversion)
</success_criteria>
<output>
02-03-SUMMARY.md
</output>
