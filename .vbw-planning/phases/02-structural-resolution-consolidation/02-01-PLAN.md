---
phase: 2
plan: 1
title: Shared command resolver, SessionStart bootstrap, and T-301 spike
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
skills_used: [running-claude-smoke-tests]
files_modified:
  - scripts/resolve-plugin-root.sh
  - scripts/session-start.sh
  - tests/resolve-plugin-root.bats
  - scripts/agent-spawn-guard.sh
  - testing/verify-agent-spawn-guard.sh
  # Scope amendment: These files were added mid-execution as a required coordination fix discovered during Task 4's full-suite verification. In a self-hosted dev checkout, agent-spawn-guard.sh could resolve an unrelated non-VBW working directory to this repo's own .vbw-planning. Commit 44a0b65f fixed the root selection and passed 108/108 spawn-guard tests plus run-all.sh.
must_haves:
  truths:
    - "scripts/resolve-plugin-root.sh implements the full nine-step command cascade including the marketplace-root scan (plugins/marketplaces/*/) and exits 1 with a diagnostic on total failure"
    - "The helper does not change scripts/hook-wrapper.sh policy (cache-first, exit 0, exit-2 pass-through stay untouched)"
    - "session-start.sh establishes the per-session link via its own SCRIPT_DIR self-location so the first command in a fresh session can reach the helper without an inline cascade"
    - "T-301 has a recorded live-runtime answer (pass, fail, or inconclusive) for both install shapes, and the probe is removed before commit"
  artifacts:
    - {path: "scripts/resolve-plugin-root.sh", provides: "single shared command-cascade resolver", contains: "plugins/marketplaces"}
    - {path: "tests/resolve-plugin-root.bats", provides: "BATS coverage for all install shapes", contains: "marketplace"}
    - {path: "scripts/session-start.sh", provides: "first-command bootstrap via ensure-plugin-root-link.sh at SessionStart", contains: "ensure-plugin-root-link"}
  key_links:
    - {from: "scripts/resolve-plugin-root.sh", to: "scripts/ensure-plugin-root-link.sh", via: "helper calls existing link script rather than reimplementing link handling"}
    - {from: "scripts/session-start.sh", to: "/tmp/.vbw-plugin-root-link-${CLAUDE_SESSION_ID}", via: "SessionStart creates the deterministic session link commands will trampoline through"}
---
<objective>
Create the single shared command-cascade resolver (scripts/resolve-plugin-root.sh), make SessionStart establish the per-session link so a fresh-session first command can reach it (the safe non-T-301-dependent baseline from research), run the bounded T-301 live spike to settle whether CLAUDE_ENV_FILE exports reach template expansions, and add direct BATS coverage. This plan unblocks the wave-2 cascade replacement.
</objective>
<context>
@.vbw-planning/phases/02-structural-resolution-consolidation/02-RESEARCH.md
@scripts/ensure-plugin-root-link.sh
@scripts/session-start.sh
@docs/wiki/plugin-root-resolution.md
@.claude/skills/running-claude-smoke-tests/SKILL.md
Rationale: research documents the exact nine-step command cascade, the two viable bootstrap entrypoints, and the requirement to keep the hook cascade (hook-wrapper.sh) untouched. ensure-plugin-root-link.sh is reused, not reimplemented (REQ-01, T-302, C1).
</context>
<tasks>
<task type="auto">
  <name>T-301 live runtime spike: CLAUDE_ENV_FILE export visibility in template expansions</name>
  <files>
    .vbw-planning/phases/02-structural-resolution-consolidation/02-SPIKE-T301.md
  </files>
  <action>
Bounded spike. Do NOT fabricate an answer statically. Follow RESEARCH.md "T-301 Spike Answer" steps exactly: (1) temporarily have session-start.sh write a sentinel export (e.g. VBW_T301_SENTINEL) to CLAUDE_ENV_FILE. (2) Add a temporary test-only command with one top-level template expansion printing the sentinel and CLAUDE_SESSION_ID. (3) Start a fresh session in BOTH a --plugin-dir install and a marketplace-root "source":"./" install and invoke the command first. (4) Record whether the values appear, session scoping, and whether marketplace-root first invocation resolves. (5) Remove the probe entirely. Record the verdict and evidence in 02-SPIKE-T301.md. Timebox: if a live run in either shape is impossible from this environment, record "inconclusive" with what was attempted. The baseline design in the next tasks does not depend on a pass.
  </action>
  <verify>
02-SPIKE-T301.md exists with a pass/fail/inconclusive verdict per install shape. `git status` shows no leftover probe code in scripts/ or commands/.
  </verify>
  <done>
Verdict recorded for both install shapes. Probe removed. No production file carries spike code.
  </done>
</task>
<task type="auto" correctness="dijkstra">
  <name>Create scripts/resolve-plugin-root.sh</name>
  <files>
    scripts/resolve-plugin-root.sh
  </files>
  <action>
Implement the command cascade exactly once, preserving the current nine-step order from RESEARCH.md: (1) CLAUDE_PLUGIN_ROOT, (2) ${VBW_CACHE_ROOT}/local, (3) highest numeric cache dir, (4) lexically-last generic cache dir, (5) marketplace-root scan under plugins/marketplaces/, (6) exact session link, (7) valid generic session-link discovery under /tmp, (8) ps axww --plugin-dir recovery, (9) fail with the existing diagnostic and exit 1. Validate the resolved directory (scripts/hook-wrapper.sh presence by default), canonicalize with pwd -P, call scripts/ensure-plugin-root-link.sh, and print the resolved root on stdout. Modes: `--require-script <name>` validates a specific script (e.g. phase-detect.sh) for the nested-refresh call sites. `--nonfatal` prints empty output and exits 0 on failure, for rtk.md's status_unavailable JSON path. Use set -euo pipefail, bash 4.4+, shellcheck-clean. Do NOT touch scripts/hook-wrapper.sh.
  </action>
  <verify>
bash -n and shellcheck -S warning pass. Manual invocations under each fixture shape (env var set, cache/local, numeric cache, generic cache, marketplace dir, exact link, generic link, no source) return the expected root or exit code.
  </verify>
  <done>
Helper exists, is executable, implements all nine steps in order, supports --require-script and --nonfatal, and exits 1 with diagnostic in default mode on total failure.
  </done>
</task>
<task type="auto">
  <name>SessionStart baseline bootstrap: create the per-session link once</name>
  <files>
    scripts/session-start.sh
  </files>
  <action>
Extend session-start.sh (near the existing cache/link bridging at :513-538) to establish the per-session link /tmp/.vbw-plugin-root-link-${CLAUDE_SESSION_ID} via ensure-plugin-root-link.sh, using its existing SCRIPT_DIR self-location (:12-19) as the root source. This is bootstrap option 2 from RESEARCH.md: commands can then reach resolve-plugin-root.sh through the deterministic session link without any inline cascade. Keep the existing local-dev cache/local and 0.0.0-marketplace bridging intact. If the T-301 spike passed, additionally export a validated canonical root variable to CLAUDE_ENV_FILE as a secondary fast path, but the session link remains the load-bearing mechanism either way.
  </action>
  <verify>
Run session-start.sh with a fixture SessionStart JSON and temp dirs: the session link exists and points at the canonical root. Existing tests/session-id-injection.bats still passes.
  </verify>
  <done>
Fresh SessionStart creates a valid session link in both --plugin-dir and marketplace-root shapes. No existing session-start behavior regresses.
  </done>
</task>
<task type="auto">
  <name>BATS coverage for the helper</name>
  <files>
    tests/resolve-plugin-root.bats
  </files>
  <action>
Add tests/resolve-plugin-root.bats covering every install shape from RESEARCH.md: explicit env var, cache/local, numeric version dir, generic cache dir, marketplace root, exact session link, valid generic link, --plugin-dir process recovery (skippable if ps is unmockable), total failure (exit 1 plus diagnostic), stale/wrong-target link repair, --require-script with present and absent script, and --nonfatal (exit 0, empty output). Add precedence tests where a higher-priority source wins when multiple exist. Leave tests/ensure-plugin-root-link.bats as the race/link-specific suite. Do not duplicate it.
  </action>
  <verify>
bats tests/resolve-plugin-root.bats passes under bash 4.4+. bash testing/run-all.sh stays green.
  </verify>
  <done>
All shape, precedence, mode, and failure cases covered and green.
  </done>
</task>
</tasks>
<verification>
1. `bash -n scripts/resolve-plugin-root.sh && shellcheck -S warning scripts/resolve-plugin-root.sh`
2. `bats tests/resolve-plugin-root.bats` green
3. `git diff --stat scripts/hook-wrapper.sh` is empty (hook cascade untouched)
4. 02-SPIKE-T301.md records a verdict per install shape
</verification>
<success_criteria>
- The command cascade is defined once in scripts/resolve-plugin-root.sh with full marketplace-root support
- SessionStart establishes the session link so a fresh-session first command can trampoline to the helper
- T-301 answered by live test (or recorded inconclusive), probe removed
</success_criteria>
<output>
02-01-SUMMARY.md
</output>
