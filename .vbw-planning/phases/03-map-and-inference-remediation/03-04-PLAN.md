---
phase: 3
plan: 4
title: Bash stack detection and project-scoped skill installs (C2, FS-1)
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
skills_used: []
files_modified: [config/stack-mappings.json, scripts/detect-stack.sh, commands/init.md, commands/skills.md, tests/detect-stack.bats, testing/verify-commands-contract.sh, testing/verify-askuserquestion-contract.sh]
forbidden_commands: []
must_haves:
  truths:
    - "A repo containing only shell scripts and Markdown yields a detected_stack that includes 'bash'"
    - "No VBW command runs 'npx skills add' with the -g or --global flag"
    - "Curated suggestion suppression uses installed.project only, so a global-only skill stays in suggestions"
  artifacts:
    - {path: "config/stack-mappings.json", provides: "bash language profile", contains: "\"*.sh\""}
    - {path: "commands/init.md", provides: "project-scoped install command", contains: "npx skills add"}
    - {path: "commands/skills.md", provides: "project-only install flow", contains: "npx skills add"}
    - {path: "tests/detect-stack.bats", provides: "bash detection and suppression-scope tests", contains: "bash"}
  key_links:
    - {from: "config/stack-mappings.json", to: "scripts/detect-stack.sh", via: "wildcard detect pattern '*.sh' consumed by the recursive find branch at lines 110-119"}
    - {from: "config/stack-mappings.json", to: "config/lsp-mappings.json", via: "canonical 'bash' stack label matches the existing LSP entry at lines 229-235"}
---
<objective>
Fix C2 and FS-1 together because both live in the stack-detection and skill-install surface and FS-1 edits scripts/detect-stack.sh. C2: config/stack-mappings.json has no bash profile, so a shell repo like VBW itself detects only github-actions and the existing bash LSP mapping is never considered. FS-1: init installs skills globally with -g, /vbw:skills offers a Global option, and detection lets a global skill suppress a project suggestion. Add the bash mapping (data fix only, no detector changes), make installs project-scoped everywhere, and scope suggestion suppression to project skills.
</objective>
<context>
@.vbw-planning/phases/03-map-and-inference-remediation/03-RESEARCH.md
@config/stack-mappings.json
@scripts/detect-stack.sh
@commands/init.md
@commands/skills.md
Rationale: RESEARCH.md sections C2 and FS-1 carry the line references and live evidence. The detector's wildcard branch already handles '*.sh' recursively, so C2 is a mapping-data fix. The skills CLI installs project-scoped by default when -g is omitted (verified against the public README).
</context>
<tasks>
<task type="auto">
  <name>Add bash language profile to stack-mappings.json</name>
  <files>
    config/stack-mappings.json
  </files>
  <action>
Add a 'bash' language entry beside the other language profiles in config/stack-mappings.json: detect pattern ["*.sh"], canonical name 'bash', an appropriate curated Bash skill recommendation, description 'Bash or shell project'. Do not modify scripts/detect-stack.sh for detection: its recursive wildcard branch at lines 110-119 already matches '*.sh'. The 'bash' label must match config/lsp-mappings.json:229-235 so init passes it to resolve-lsp.sh. Addresses C2. Edit JSON with jq-verified syntax (jq . config/stack-mappings.json must parse).
  </action>
  <verify>
bash scripts/detect-stack.sh . in this repo reports detected_stack including both 'bash' and 'github-actions'. jq . config/stack-mappings.json exits 0.
  </verify>
  <done>
A shell repo gets a real stack profile including the bash entry and its skill recommendation.
  </done>
</task>
<task type="auto">
  <name>Make init skill installs project-scoped</name>
  <files>
    commands/init.md
  </files>
  <action>
Change the unified install command at commands/init.md:321 from 'npx skills add <skill> -g -y' to 'npx skills add <skill> -y'. The skills CLI defaults to project scope when -g is omitted. Check the surrounding prompt text (lines 319-322) for any wording that promises global installation and align it with project scope. Addresses FS-1.
  </action>
  <verify>
grep -n 'skills add' commands/init.md shows no -g or --global flag.
  </verify>
  <done>
Init installs skills into the current project only.
  </done>
</task>
<task type="auto">
  <name>Make /vbw:skills project-only</name>
  <files>
    commands/skills.md
  </files>
  <action>
Remove the Global scope option at commands/skills.md:103-110 and the SCOPE branch at line 113, retaining only the no-flag project install command. Do not merely reorder options: the Global path must be gone so users cannot select it. Update the installed-skill listing guidance at line 41 so suppression context refers to project-installed skills, with global skills at most informational. Addresses FS-1.
  </action>
  <verify>
grep -n '\-g\|--global' commands/skills.md shows no install command using a global flag and no selectable Global option.
  </verify>
  <done>
/vbw:skills offers exactly one install scope: the project.
  </done>
</task>
<task type="auto">
  <name>Scope suggestion suppression to project-installed skills</name>
  <files>
    scripts/detect-stack.sh
  </files>
  <action>
Change scripts/detect-stack.sh so curated suggestion suppression at lines 173-185 uses installed.project only, not the combined set built at lines 25-36. Keep collecting global skills for informational output (installed.global stays in the JSON), but a globally installed skill must no longer remove its match from suggestions. Addresses FS-1. Keep shellcheck -S warning clean.
  </action>
  <verify>
With a fixture where the recommended skill exists only globally, detect-stack output keeps that skill in suggestions while listing it under installed.global.
  </verify>
  <done>
Global availability never satisfies a project skill dependency.
  </done>
</task>
<task type="auto">
  <name>Add detection tests and a global-flag contract guard</name>
  <files>
    tests/detect-stack.bats
    testing/verify-commands-contract.sh
  </files>
  <action>
Tier 1 BATS in tests/detect-stack.bats: (1) temp repo with only an executable scripts/main.sh and a README asserts detected_stack includes 'bash'. (2) empty temp repo asserts 'bash' absent. (3) temp repo with scripts/main.sh plus .github/workflows/ci.yml asserts both 'bash' and 'github-actions' (supplement, not replace). (4) global-only skill fixture asserts the skill remains in suggestions. Tier 2 contract check in testing/verify-commands-contract.sh: fail when commands/init.md or commands/skills.md contains 'npx skills add' with '-g' or '--global'. Addresses C2 and FS-1 verification.
  </action>
  <verify>
bats tests/detect-stack.bats passes, bash testing/verify-commands-contract.sh passes, and bash testing/run-all.sh stays green.
  </verify>
  <done>
Behavior tests pin bash detection and project-only suppression, and the contract check blocks any future global-install regression.
  </done>
</task>
</tasks>
<verification>
1. bash scripts/detect-stack.sh . reports 'bash' alongside 'github-actions' for this repo.
2. grep across commands/init.md and commands/skills.md finds no global-scope skills install.
3. bash testing/run-all.sh passes.
</verification>
<success_criteria>
- A shell repo gets a real stack profile (phase success criterion).
- Skills install project-scoped from both init and /vbw:skills (phase success criterion).
- Global skills never suppress project suggestions, and a contract test enforces the no-global-flag rule.
</success_criteria>
<output>
03-04-SUMMARY.md
</output>

## Deviation amendment (QA round 01)

Commit 160525a2 rewrote an assertion in testing/verify-askuserquestion-contract.sh. Removing the Global scope step from commands/skills.md made the prior AskUserQuestion contract stale, so the assertion had to track the project-only flow and keep the suite green.
