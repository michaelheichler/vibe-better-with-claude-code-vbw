---
phase: 2
plan: 3
title: qa-result-gate correctness for known-issue dedup and doc-content fixes
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
skills_used: []
files_modified: [scripts/lib/qa-result-gate-known-issues.sh, scripts/lib/qa-result-gate-path-evidence.sh, scripts/qa-result-gate.sh, tests/qa-result-gate.bats, references/execute-qa-result-gating.md, references/vibe-input-parsing.md, templates/REMEDIATION-PLAN.md]
files_touched: [scripts/lib/qa-result-gate-known-issues.sh, scripts/lib/qa-result-gate-path-evidence.sh, scripts/lib/qa-result-gate-fail-classifications.sh, scripts/qa-result-gate.sh, tests/qa-result-gate.bats, references/execute-qa-result-gating.md, references/vibe-input-parsing.md, templates/REMEDIATION-PLAN.md]
forbidden_commands: []
must_haves:
  truths:
    - "A deduped known_issues_input covering every (test, file) pair with one raw error variant passes coverage (REQ-14, STATE.md line 31)"
    - "A genuinely missing (test, file) pair still fails closed (tests/qa-result-gate.bats:5190)"
    - "A doc-content FAIL fixed by editing that same doc resolves without the process-exception workaround (STATE.md line 43)"
    - "Unrelated doc/README churn still never satisfies code-fix evidence (tests/qa-result-gate.bats:3764, :3812)"
  artifacts:
    - {path: "scripts/lib/qa-result-gate-known-issues.sh", provides: "(test,file)-scoped coverage with error-variant tolerance", contains: "issue_key"}
    - {path: "scripts/lib/qa-result-gate-path-evidence.sh", provides: "doc-content-fix recognition", contains: "path_is_documentation_artifact"}
    - {path: "tests/qa-result-gate.bats", provides: "new regression tests for both fixes", contains: "doc"}
  key_links:
    - {from: "scripts/qa-result-gate.sh", to: "scripts/lib/qa-result-gate-known-issues.sh", via: "coverage call at line 440"}
    - {from: "scripts/qa-result-gate.sh", to: "scripts/lib/qa-result-gate-path-evidence.sh", via: "code-fix evidence and METADATA_ONLY_ROUND checks at lines 519-543"}
---
<objective>
Fix root causes 2 and 3 from 02-RESEARCH.md in the qa-result-gate subsystem.
First: known-issues coverage demands byte-exact (test, file, error) triples.
A Lead deduping two error wordings for one test+file pair gets forced into a spurious remediation round (STATE.md line 31, ref:698a808b).
Second: every doc path is classified metadata.
A remediation round whose FAIL is a documentation-content defect can never present valid fix evidence by editing that doc (STATE.md line 43, ref:2a7c918e).
</objective>
<context>
@.vbw-planning/phases/02-qa-and-verification-gate-correctness/02-RESEARCH.md
@scripts/qa-result-gate.sh
@scripts/lib/qa-result-gate-known-issues.sh
@scripts/lib/qa-result-gate-path-evidence.sh

Known-issues fix site: `json_object_array_covers_full_issue_objects` (qa-result-gate-known-issues.sh:96-151).
It is called from qa-result-gate.sh:440 with the raw registry loaded at line 422.
A mismatch routes REMEDIATION_REQUIRED at lines 508-511.
Doc-fix sites: `path_is_documentation_artifact` (path-evidence.sh:140-162), `path_is_metadata_artifact` (:177-188), `path_is_code_fix_support_artifact` (:190-204), `path_is_process_exception_evidence_artifact` (:206-213), `paths_include_code_fix_evidence` (:505-518), consumed at qa-result-gate.sh:519 and 533-543.
Classification precedent: `fail_classifications` type values (code-fix, plan-amendment, process-exception) in qa-result-gate-fail-classifications.sh. Adding a doc-fix value follows this existing pattern.
Both fixes share qa-result-gate.sh and tests/qa-result-gate.bats, which is why they live in one plan.
Update any reference doc that enumerates classification values (for example references/verification-protocol.md) if a new value is added.
</context>
<tasks>
<task type="auto" correctness="dijkstra">
  <name>Relax known-issues coverage to (test,file)-scoped with error-variant semantics</name>
  <files>
    scripts/lib/qa-result-gate-known-issues.sh
    scripts/qa-result-gate.sh
  </files>
  <action>
Change the coverage semantics used at qa-result-gate.sh:440 (in `json_object_array_covers_full_issue_objects` or a dedicated variant for that call site).
Required invariant, per (test, file) pair in the required registry: the candidate contains an entry for that pair.
That entry's error equals at least one raw error variant the registry holds for that pair.
That accepts a Lead's dedup of multiple error wordings into one kept-first entry.
It rejects a candidate whose error matches none of the raw variants (wrong-error substitution) and rejects any missing pair.
Derive the loop from this invariant explicitly (Dijkstra discipline): state the pair-grouping precondition, the per-pair membership check, and the termination condition over the required set. REQ-14.
  </action>
  <verify>
Run the known-issues sections of tests/qa-result-gate.bats, in particular the test at line 5190.
  </verify>
  <done>
Deduped-variant coverage passes, missing-pair and wrong-error inputs still fail closed, test at 5190 unmodified and green.
  </done>
</task>
<task type="auto">
  <name>Add regression tests for dedup tolerance</name>
  <files>
    tests/qa-result-gate.bats
  </files>
  <action>
Add tests proving the previously failing case now passes.
Fixture: a registry with two error wordings for one (test, file) pair, and a known_issues_input carrying that pair once with the first error wording.
Expected: resolves without REMEDIATION_REQUIRED from coverage.
Add the adversarial case: same pair present but with an error string matching no raw variant must still fail closed.
Confirm tests/qa-result-gate.bats:5190 (subset-coverage fails closed) passes unmodified. REQ-14.
  </action>
  <verify>
Run tests/qa-result-gate.bats known-issues tests green, including line 5190 and the new tests.
  </verify>
  <done>
New dedup-tolerance and wrong-error tests green, existing 5190 guarantee intact.
  </done>
</task>
<task type="auto">
  <name>Recognize doc-content fixes in path evidence and classification</name>
  <files>
    scripts/lib/qa-result-gate-path-evidence.sh
    scripts/lib/qa-result-gate-fail-classifications.sh
    scripts/qa-result-gate.sh
  </files>
  <action>
Add a targeted escape hatch for the case where the doc is the product.
Preferred direction per research: a distinct `doc-fix` classification value (following the existing fail-classifications pattern), or a predicate that accepts a changed doc path when it matches the doc path named in the FAIL's own evidence or description.
A round classified for a doc-content defect whose changed paths include the named doc must satisfy fix evidence at qa-result-gate.sh:519.
It must not be trapped by the METADATA_ONLY_ROUND branch at lines 533-543.
Unrelated doc churn under a code-fix classification must keep failing exactly as today.
Update the classification-value enumeration anywhere it is documented (fail-classifications lib validation plus any references/*.md list). REQ-14.
  </action>
  <verify>
bash -n and shellcheck -S warning on the three edited scripts. Run the path-evidence and metadata-round sections of tests/qa-result-gate.bats.
  </verify>
  <done>
A doc-content FAIL fixed by editing the named doc routes to a non-blocking verdict. Code-fix FAILs still reject doc-only evidence.
  </done>
</task>
<task type="auto">
  <name>Add regression tests for the doc-content-fix path</name>
  <files>
    tests/qa-result-gate.bats
  </files>
  <action>
Add a test proving the previously failing case now passes: a FAIL whose subject is documentation content, fixed by editing that same doc file only, resolves to PROCEED_TO_UAT (or the correctly labeled non-blocking verdict) without the process-exception workaround.
Add the anti-abuse case if not already covered: the new mechanism must not accept a doc edit for a FAIL about production code.
Confirm tests/qa-result-gate.bats:3764 and :3812 pass unmodified. REQ-14.
  </action>
  <verify>
Full tests/qa-result-gate.bats run green, including 3764, 3812, 5190, and all new tests.
  </verify>
  <done>
Doc-content-fix acceptance and anti-abuse rejection both proven by tests, existing locked-in tests untouched and green.
  </done>
</task>
</tasks>
<verification>
1. bash -n and shellcheck -S warning on all edited scripts
2. Full tests/qa-result-gate.bats green, with tests at lines 3764, 3812, and 5190 unmodified
3. New tests cover: dedup tolerance pass, wrong-error fail, missing-pair fail, doc-content-fix pass, doc-churn-for-code-FAIL fail
</verification>
<success_criteria>
- STATE.md line 31 (ref:698a808b) closed: deduped known-issue carryover passes coverage without a spurious remediation round
- STATE.md line 43 (ref:2a7c918e) closed: doc-content fixes present valid evidence without the process-exception misclassification
- Anti-abuse properties of the gate preserved (locked-in tests unmodified and green)
</success_criteria>
<output>
02-03-SUMMARY.md
</output>
