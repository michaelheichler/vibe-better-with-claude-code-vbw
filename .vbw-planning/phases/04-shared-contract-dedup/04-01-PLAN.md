---
phase: 4
plan: 01
title: Canonical subagent-contracts doc, agent shutdown rewrite, Phase-6 inventory
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used: [plugin-dev:command-development]
files_modified: [references/subagent-contracts.md, agents/vbw-scout.md, agents/vbw-docs.md, agents/vbw-qa.md, agents/vbw-dev.md, agents/vbw-lead.md, agents/vbw-debugger.md, .vbw-planning/STATE.md]
files_touched: [references/subagent-contracts.md, agents/vbw-scout.md, agents/vbw-docs.md, agents/vbw-qa.md, agents/vbw-dev.md, agents/vbw-lead.md, agents/vbw-debugger.md, .vbw-planning/STATE.md]
forbidden_commands: []
must_haves:
  truths:
    - "references/subagent-contracts.md contains three named canonical contract sections: team-shutdown, non-team spawn shape, no-tool circuit breaker (REQ-08)"
    - "Each contract section defines exactly one canonical invariant sentence in a fenced block, marked as the byte-exact string call sites must copy"
    - "All six participating agent files keep role-specific stop tails and Debugger checkpoint guidance inline, so behavior is preserved, not deleted (REQ-10)"
    - "agents/vbw-architect.md shutdown exclusion is untouched"
    - "The sentence 'Plain text acknowledgement is NOT sufficient.' appears in references/subagent-contracts.md and in no agent file"
  artifacts:
    - {path: "references/subagent-contracts.md", provides: "canonical text of all three contracts plus the three invariant sentences", contains: "Shutdown invariant:"}
    - {path: "agents/vbw-dev.md", provides: "condensed shutdown section with pointer plus canonical invariant", contains: "Shutdown invariant:"}
    - {path: ".vbw-planning/STATE.md", provides: "Phase-6 skill-activation inventory recorded as todos", contains: "Phase-6 inventory"}
  key_links:
    - {from: "agents/vbw-scout.md", to: "references/subagent-contracts.md", via: "pointer line naming the canonical doc"}
    - {from: ".vbw-planning/STATE.md", to: "04-RESEARCH.md Phase 6 Skill-Block Inventory", via: "todo entries summarizing the 20 payload-prefix pairs and adjacent bugs"}
---
<objective>
Create `references/subagent-contracts.md` as the single canonical home for the team-shutdown contract, the non-team spawn-shape boilerplate, and the subagent no-tool circuit breaker. Condense the six participating agent shutdown sections to pointer plus canonical invariant while preserving all role-specific behavior. Record the Phase-6 skill-activation inventory in STATE.md todos. Serves REQ-08 (centralize shutdown contract), REQ-10 (preserve behavior), and REQ-11 (root-cause dedup, not masking).
</objective>
<context>
@.vbw-planning/phases/04-shared-contract-dedup/04-CONTEXT.md
@.vbw-planning/phases/04-shared-contract-dedup/04-RESEARCH.md
@/Users/michael/.claude/plugins/cache/claude-plugins-official/plugin-dev/unknown/skills/command-development/SKILL.md
Rationale: 04-CONTEXT.md holds the binding user decisions (pointer plus one byte-identical invariant sentence per contract, redundancy over pure pointers). 04-RESEARCH.md holds the exact file and line inventory of every duplicate and the drift notes. The command-development skill documents the standalone `@`-include semantics the later plans depend on, and the canonical doc written here must be structured for that delivery.
</context>
<tasks>
<task type="auto">
  <name>Write references/subagent-contracts.md</name>
  <files>
    references/subagent-contracts.md
  </files>
  <action>
Create the canonical doc with exactly three top-level contract sections, in this order: `## Team-Shutdown Contract`, `## Non-Team Spawn Shape`, `## No-Tool Circuit Breaker`.

For each section include two parts. Part (a) is the full canonical prose consolidated from the strongest existing copy per 04-RESEARCH.md. For shutdown, that is the generic request-detection procedure, the SendMessage-mandatory response with the response JSON example, and the `CRITICAL: Plain text acknowledgement is NOT sufficient.` paragraph. For non-team, that is the full spawn-shape sentence forbidding `team_name`, `run_in_background`, `isolation`, and worktree cwd fields (`cwd`, `working_dir`, `workingDirectory`, `workdir`) plus the rule that `name` is optional label-only metadata, standardized on `never use it`. For no-tool, that is the generic breaker built on the marker phrase `tools, shell/Bash, filesystem, edits, or API-session access are unavailable`, standardized on `do not retry the same prompt`, including the vibe.md-only sentence `Repeating a no-tool spawn cannot fix tool provisioning and wastes tokens.` Part (b) is one canonical invariant sentence in its own fenced code block, introduced by a line stating call sites must copy it byte-for-byte. Use these three invariants exactly:

```
Shutdown invariant: acknowledge every `shutdown_request` by calling SendMessage with `shutdown_response`, then stop.
Non-team invariant: omit `team_name`, `run_in_background`, `isolation`, and all worktree cwd fields.
No-tool invariant: treat unavailable tools as a provisioning failure, do not advance state, and do not retry the same prompt.
```

State explicitly in the doc header which material stays local at call sites: role-specific stop tails, Debugger checkpointing, the Architect shutdown exclusion, actual-team-mode gating and teardown sequencing, residual-cleanup ordering, UAT artifact-path guidance, and any text pasted verbatim into child prompt payloads. Do not duplicate the V2 shutdown schemas from references/handoff-schemas.md. Name that file as the schema authority. ASCII punctuation only, no em dashes.
  </action>
  <verify>
grep -c '^## ' references/subagent-contracts.md returns 3. grep -F 'Shutdown invariant:' and 'Non-team invariant:' and 'No-tool invariant:' each match. grep -F 'Plain text acknowledgement is NOT sufficient.' matches.
  </verify>
  <done>
Canonical doc exists with three sections, three fenced invariant sentences, full canonical prose, and an explicit stays-local list. Committed as docs(references): add canonical subagent-contracts doc.
  </done>
</task>
<task type="auto">
  <name>Condense six agent shutdown sections to pointer plus invariant</name>
  <files>
    agents/vbw-scout.md
    agents/vbw-docs.md
    agents/vbw-qa.md
    agents/vbw-dev.md
    agents/vbw-lead.md
    agents/vbw-debugger.md
  </files>
  <action>
In each of the six `## Shutdown Handling` sections (line ranges in 04-RESEARCH.md), replace the generic duplicated middle (the three-step detection and response procedure prose and the `CRITICAL: Plain text acknowledgement is NOT sufficient.` paragraph) with three elements: (1) a pointer line naming `references/subagent-contracts.md` under the plugin root as the canonical shutdown contract, instructing the agent to read it if the full procedure is needed, (2) the canonical shutdown invariant sentence copied byte-for-byte from the doc written in task 1, and (3) the existing response JSON example retained inline so the agent can respond without a file read (JSON schema fields are not anti-drift markers).

Preserve unchanged: each agent's role-specific stop tail (Scout searches, Docs doc tasks, QA checks, Dev tasks, Lead plans) and the Debugger checkpoint-progress guidance. Do not touch agents/vbw-architect.md (the deliberate exclusion stays inline). The invariant line must be byte-identical across all six files. Do not edit any other section of the agent files.
  </action>
  <verify>
grep -F 'Shutdown invariant: acknowledge every' agents/vbw-*.md matches exactly 6 files, none of them vbw-architect.md, and all 6 matched lines are byte-identical (sort -u of the matched lines yields 1 line). grep -rF 'Plain text acknowledgement is NOT sufficient.' agents/ matches 0 files. Each of the six files still contains its role-specific stop tail (spot-check 'Do NOT start new' and the Debugger 'Checkpoint' line).
  </verify>
  <done>
Six agent shutdown sections are pointer plus invariant plus retained JSON example plus role tail. Committed as refactor(agents): point shutdown handling at canonical contract.
  </done>
</task>
<task type="auto">
  <name>Record Phase-6 skill-activation inventory in STATE.md todos</name>
  <files>
    .vbw-planning/STATE.md
  </files>
  <action>
Append to the `## Todos` section of .vbw-planning/STATE.md a compact Phase-6 inventory block derived from the `## Phase 6 Skill-Block Inventory` section of 04-RESEARCH.md. Include: (1) one todo stating there are 20 skill-activation payload-prefix pairs (40 blocks) across research.md, fix.md, map.md, qa.md, debug.md, vibe.md (10 pairs), and execute-protocol.md, with a pointer to 04-RESEARCH.md for the exact line table, (2) one todo per adjacent inconsistency, namely the map.md `Skills: none preselected.` period-versus-comma wording drift at lines 195 and 230, and the vibe.md UAT research path (line 808) selecting only directly needed skills contrary to the Scout additive-selection rule, and (3) a note that the seven agent-level `## Skill Activation` sections are parsing rules, not payload templates, and that the execute-protocol.md pair is literal child-prompt text that must stay inline. Tag each entry with (Phase-6 inventory, added 2026-08-02). Do not modify any other STATE.md section.
  </action>
  <verify>
grep -c 'Phase-6 inventory' .vbw-planning/STATE.md returns at least 3. Existing todos above the new block are unchanged (git diff shows additions only).
  </verify>
  <done>
STATE.md Todos carries the Phase-6 inventory and adjacent-bug entries. Committed as docs(state): record phase-6 skill-activation inventory.
  </done>
</task>
</tasks>
<verification>
1. references/subagent-contracts.md exists with three contract sections and three fenced invariant sentences.
2. grep -rF 'Plain text acknowledgement is NOT sufficient.' agents/ returns nothing, so the sentence lives only in references/subagent-contracts.md.
3. The shutdown invariant line is byte-identical in the canonical doc and all six agent files.
4. STATE.md Todos contains the Phase-6 inventory entries and no prior todo was altered.
5. bash testing/run-all.sh stays green (no contract test covers the new doc yet, and existing agent contract tests must still pass).
</verification>
<success_criteria>
- The canonical shutdown, non-team, and no-tool contract prose lives in exactly one references doc.
- All six participating agents preserve full shutdown behavior via pointer, invariant, retained JSON example, and role tail.
- Phase 6 starts with a recorded inventory instead of a rescan.
</success_criteria>
<output>
04-01-SUMMARY.md
</output>

## Amendments (R01)

1. **DEV-01:** Task 2's constraint, "Do not edit any other section of the agent files," is relaxed for discipline-watcher hook remediation. Commit `235dae0a` includes required punctuation, sentence, and list cleanup in `agents/vbw-dev.md`, `agents/vbw-lead.md`, `agents/vbw-qa.md`, and `agents/vbw-debugger.md`. The hook blocks turn completion while findings remain in modified files.
2. **DEV-02:** Task 2's condensed-section element list now permits a fifth element. The inline call-SendMessage marker sentence, "A plain-text reply is NOT sufficient," remains in `agents/vbw-debugger.md` and `agents/vbw-scout.md` because `tests/shutdown-protocol.bats` asserts it. This marker differs from the prohibited canonical sentence, "Plain text acknowledgement is NOT sufficient." MH-05 and the anti-drift verifier therefore still hold.
