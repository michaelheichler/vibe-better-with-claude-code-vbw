---
phase: 4
plan: 02
title: Command and reference call-site rewrites with shared-contract includes
type: execute
wave: 2
depends_on: [04-01]
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [plugin-dev:command-development]
files_modified:
  - commands/vibe.md
  - commands/debug.md
  - commands/map.md
  - commands/qa.md
  - commands/fix.md
  - commands/research.md
  - references/execute-protocol.md
  - references/handoff-schemas.md
  - testing/verify-commands-contract.sh
  - testing/verify-execute-delegation-routing.sh
  - testing/verify-uat-remediation-orchestration.sh
  - testing/verify-debug-session-contract.sh
  - tests/shutdown-protocol.bats
files_touched:
  - commands/vibe.md
  - commands/debug.md
  - commands/map.md
  - commands/qa.md
  - commands/fix.md
  - commands/research.md
  - references/execute-protocol.md
  - references/handoff-schemas.md
  - testing/verify-commands-contract.sh
  - testing/verify-execute-delegation-routing.sh
  - testing/verify-uat-remediation-orchestration.sh
  - testing/verify-debug-session-contract.sh
  - tests/shutdown-protocol.bats
forbidden_commands: []
must_haves:
  truths:
    - "All six subagent-spawning commands carry the standalone include line @${CLAUDE_PLUGIN_ROOT}/references/subagent-contracts.md on its own line, not embedded in prose"
    - "references/execute-protocol.md carries a read directive for references/subagent-contracts.md, not an @-include"
    - "Every replaced call site keeps a pointer line plus the byte-identical canonical invariant sentence for its contract"
    - "Team-mode gating, teardown sequencing, residual cleanup, role settings, UAT artifact-path guidance, and child-prompt payload text stay inline (REQ-10)"
    - "The V2 shutdown schemas and backward-compatibility rules in references/handoff-schemas.md are unchanged"
  artifacts:
    - {path: "commands/vibe.md", provides: "include line plus rewritten call sites for all three contracts", contains: "@${CLAUDE_PLUGIN_ROOT}/references/subagent-contracts.md"}
    - {path: "commands/fix.md", provides: "include line plus non-team invariant at the Dev spawn", contains: "Non-team invariant: omit"}
    - {path: "references/execute-protocol.md", provides: "read directive plus rewritten shutdown, non-team, and no-tool call sites", contains: "subagent-contracts.md"}
  key_links:
    - {from: "commands/vibe.md", to: "references/subagent-contracts.md", via: "standalone @-include"}
    - {from: "references/execute-protocol.md", to: "references/subagent-contracts.md", via: "runtime read directive"}
---
<objective>
Rewrite every duplicated call site of the three centralized contracts across the six spawning commands and the runtime references. Each site becomes a pointer plus the byte-identical canonical invariant sentence, with all site-specific behavior retained inline. Deliver the canonical doc to commands via a standalone `@`-include and to execute-protocol.md via a read directive. Serves REQ-08, REQ-10, REQ-11.
</objective>
<context>
@.vbw-planning/phases/04-shared-contract-dedup/04-CONTEXT.md
@.vbw-planning/phases/04-shared-contract-dedup/04-RESEARCH.md
@references/subagent-contracts.md
@/Users/michael/.claude/plugins/cache/claude-plugins-official/plugin-dev/unknown/skills/command-development/SKILL.md
Rationale: 04-RESEARCH.md is the authoritative call-site inventory with line numbers and per-site retain lists. references/subagent-contracts.md (written by plan 04-01) defines the three canonical invariant sentences that must be copied byte-for-byte. The command-development skill and the repo template-expansion rule require the include to be a standalone line, because embedded `@` spans inside prose do not load.
</context>
<tasks>
<task type="auto">
  <name>Add includes to six commands and read directive to execute-protocol</name>
  <files>
    commands/vibe.md
    commands/debug.md
    commands/map.md
    commands/qa.md
    commands/fix.md
    commands/research.md
    references/execute-protocol.md
  </files>
  <action>
In each of the six commands, add this exact standalone line near the shared-context material at the top of the body, before any spawning instructions (vibe.md already shows the correct placement pattern with its ask-user-question include at line 14):

@${CLAUDE_PLUGIN_ROOT}/references/subagent-contracts.md

The line must be its own line with no surrounding prose. In references/execute-protocol.md, do NOT add an `@`-include (the file is itself read at runtime and would double-load). Instead add a directive sentence near the top instructing the reader to read `references/subagent-contracts.md` under the plugin root for the canonical subagent contracts before spawning. Make no other edits in this task.
  </action>
  <verify>
grep -Fx '@${CLAUDE_PLUGIN_ROOT}/references/subagent-contracts.md' commands/vibe.md commands/debug.md commands/map.md commands/qa.md commands/fix.md commands/research.md matches once per file. grep -F 'subagent-contracts.md' references/execute-protocol.md matches a read directive and grep -F '@${CLAUDE_PLUGIN_ROOT}/references/subagent-contracts.md' references/execute-protocol.md finds no standalone include line. bash testing/verify-no-inline-exec-spans.sh passes.
  </verify>
  <done>
Six commands carry the standalone include, execute-protocol.md carries the read directive. Committed as feat(commands): include canonical subagent-contracts reference.
  </done>
</task>
<task type="auto">
  <name>Rewrite team-shutdown call sites in commands and references</name>
  <files>
    commands/debug.md
    commands/map.md
    commands/vibe.md
    references/execute-protocol.md
    references/handoff-schemas.md
  </files>
  <action>
At each shutdown call site listed in 04-RESEARCH.md (debug.md Path A teardown 249-253, map.md Step 5 line 273, vibe.md Pure-Vibe loop 1720, execute-protocol.md Step 3 line 138 and Step 5 lines 967-977, handoff-schemas.md Delivery format 328-334), replace the generic duplicated procedure prose with a pointer to `references/subagent-contracts.md` plus the canonical shutdown invariant sentence copied byte-for-byte from that doc.

Retain inline at each site: debug.md teardown ordering before post-synthesis implementation, map.md mode-specific solo-skip and zero-teammate verification, vibe.md delegation_mode=team guard forbidding shutdown in serialized, direct, or fallback mode, execute-protocol.md actual-team gating, event logging, residual cleanup, and state sequencing, and handoff-schemas.md schema fields, normalization, and backward-compatibility rules. Do not touch execute-protocol.md lines 391 and 403-404 (event-log terminology) or the V2 schema blocks 284-325 in handoff-schemas.md. Keep the invariant line byte-identical at every site.
  </action>
  <verify>
grep -rF 'Shutdown invariant: acknowledge every' commands/ references/ | grep -v subagent-contracts.md returns the rewritten sites, and sort -u of the matched invariant text yields 1 line identical to the canonical doc. grep -rF 'Plain text acknowledgement is NOT sufficient.' commands/ references/ matches only references/subagent-contracts.md. The retained guards still exist (spot-check delegation_mode=team in vibe.md and the actual-team gate in execute-protocol.md Step 5).
  </verify>
  <done>
All shutdown call sites are pointer plus invariant with site-specific gating intact. Committed as refactor(commands): centralize team-shutdown contract prose.
  </done>
</task>
<task type="auto">
  <name>Rewrite non-team spawn-shape call sites</name>
  <files>
    commands/research.md
    commands/fix.md
    commands/qa.md
    commands/debug.md
    commands/vibe.md
    references/execute-protocol.md
  </files>
  <action>
At each of the 21 non-team boilerplate sites listed in 04-RESEARCH.md (research.md 83, fix.md 83, qa.md 153 and 330, debug.md 254, 297, 462, vibe.md 328, 385, 593, 751, 761, 808, 854, 919, 1206, 1281, 1526, 1580, execute-protocol.md 706 and 748), replace the full spawn-shape boilerplate with the canonical non-team invariant sentence copied byte-for-byte from references/subagent-contracts.md, relying on the include or read directive from the first task for the full contract.

Retain inline at each site the contextual text named in the research table: role model, max-turn, effort, and timeout settings, fresh-instance and sole-owner contracts (debug.md 254), TodoWrite and absolute host-path rationale (vibe.md 751, execute-protocol.md 706), NO-team-creation and sequential-execution rules (vibe.md 854, 919), and skill-payload or prompt instructions. Where the boilerplate is embedded in a literal child-prompt payload or example that is pasted verbatim into a spawned subagent prompt (vibe.md 761 example, 808 and 1580 parentheticals if payload-embedded), keep the text inline unchanged per the binding scope decision, and note each kept site in the SUMMARY deviations. Do not touch the true-team rules at execute-protocol.md 293 and 320-321. Do not touch map.md 168 and 224 (team-only paths without full boilerplate).
  </action>
  <verify>
grep -rF 'Non-team invariant: omit' commands/ references/ | grep -v subagent-contracts.md covers the rewritten sites, and sort -u of the invariant text yields 1 line. grep -rF 'Non-team spawn shape: omit' commands/ references/ matches only references/subagent-contracts.md plus any documented payload-verbatim keeps.
  </verify>
  <done>
Non-team boilerplate is centralized with contextual settings and payload-verbatim text intact. Committed as refactor(commands): centralize non-team spawn-shape contract.
  </done>
</task>
<task type="auto">
  <name>Rewrite no-tool circuit-breaker call sites</name>
  <files>
    references/execute-protocol.md
    commands/vibe.md
  </files>
  <action>
At each of the 11 breaker sites listed in 04-RESEARCH.md (execute-protocol.md 185-189, 708-710, 750, 772, 779, vibe.md 330-332, 387, 428, 458, 798, 1009), replace the generic breaker prose with the canonical no-tool invariant sentence copied byte-for-byte from references/subagent-contracts.md plus a pointer where the site is a full behavioral duplicate.

Retain inline at each site: the stage-specific next-prohibited-action clause (before plan validation, before summary check, before known-issue sync and deterministic gate), the UAT `.uat-remediation-stage` state context at vibe.md 798, the quick-fix `do not enter re-verification` addition at vibe.md 1009, and the neighboring blocker-report and partial-work recovery branches at execute-protocol.md 185-189. This resolves the wording drift (same prompt shape versus same prompt) by standardizing on the canonical sentence. Generic agent `## Circuit Breaker` sections (three-consecutive-errors rule) are out of scope, leave them untouched.
  </action>
  <verify>
grep -rF 'No-tool invariant: treat unavailable tools' commands/ references/ | grep -v subagent-contracts.md covers the rewritten sites and sort -u of the invariant text yields 1 line. grep -rF 'tools, shell/Bash, filesystem, edits, or API-session access are unavailable' commands/ references/ matches only references/subagent-contracts.md. Stage-specific clauses still present (spot-check vibe.md 798 UAT state file and 1009 re-verification clause).
  </verify>
  <done>
All 11 breaker sites carry the byte-identical canonical invariant with stage context intact. Committed as refactor(commands): centralize no-tool circuit-breaker contract.
  </done>
</task>
</tasks>
<verification>
1. The standalone include appears exactly once in each of the six spawning commands and nowhere embedded in prose.
2. execute-protocol.md has a read directive, not an include.
3. For each contract, the distinctive full-prose marker appears only in references/subagent-contracts.md (allowing documented payload-verbatim keeps for non-team).
4. All copied invariant lines are byte-identical to the canonical sentences (sort -u per invariant yields one line).
5. bash testing/run-all.sh stays green, including verify-commands-contract.sh and verify-no-inline-exec-spans.sh.
</verification>
<success_criteria>
- Every duplicated contract block in commands and references is replaced by pointer plus canonical invariant with zero behavior loss.
- The canonical doc is delivered to all consumers through the correct mechanism per file type.
</success_criteria>
<output>
04-02-SUMMARY.md
</output>

## Amendments (R01)

**DEV-04:** Commit `260d0560` updated five verifier files. Those verifiers held stale duplicated-prose assertions that the full suite exposed after centralization. Updating them was required by the zero-tolerance test policy. The scope expansion was declared in the `04-02-SUMMARY.md` deviations.
