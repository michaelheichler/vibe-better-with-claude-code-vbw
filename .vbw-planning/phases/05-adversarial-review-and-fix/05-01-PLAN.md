---
phase: 5
plan: 1
title: Fix adversarial review findings 1-5
type: execute
wave: 1
depends_on: []
cross_phase_deps:
  - {phase: 2, plan: "02-01", artifact: "scripts/resolve-plugin-root.sh", reason: "findings 1-3 harden the Phase 02 shared command resolver"}
  - {phase: 3, plan: "03-01", artifact: "config/stack-mappings.json", reason: "finding 4 fixes the mapping file Phase 03 stack detection relies on"}
autonomous: true
effort_override: balanced
skills_used: []
files_modified: [scripts/resolve-plugin-root.sh, tests/resolve-plugin-root.bats, config/stack-mappings.json, scripts/session-start.sh]
files_touched: [scripts/resolve-plugin-root.sh, tests/resolve-plugin-root.bats, config/stack-mappings.json, scripts/detect-stack.sh, scripts/session-start.sh, tests/detect-stack.bats, tests/session-start.bats, testing/run-all.sh, testing/verify-plugin-root-resolution.sh]
forbidden_commands: []
must_haves:
  truths:
    - "Cache resolution selects the newest VALID candidate, not the newest candidate: an invalid newer cache entry never masks a usable older one"
    - "Marketplace and process-derived roots are accepted only when .claude-plugin/plugin.json identifies the plugin as vbw"
    - "A --plugin-dir path containing spaces is recovered intact by the process fallback"
    - "jq -c '.mobile | keys' config/stack-mappings.json lists android-kotlin, flutter, react-native, ios-swift, and swiftui in one object"
    - "SessionStart cache cleanup orders versions correctly on BSD sort without -V support"
  artifacts:
    - {path: "scripts/resolve-plugin-root.sh", provides: "candidate iteration and identity validation", contains: "plugin.json"}
    - {path: "tests/resolve-plugin-root.bats", provides: "regression coverage for findings 1-3", contains: "invalid"}
    - {path: "config/stack-mappings.json", provides: "single merged mobile category", contains: "android-kotlin"}
    - {path: "scripts/session-start.sh", provides: "portable version sort in cache cleanup", contains: "sort -V 2>/dev/null"}
  key_links:
    - {from: "tests/resolve-plugin-root.bats", to: "scripts/resolve-plugin-root.sh", via: "BATS cases exercising invalid-newest, collision, and quoted-path scenarios"}
    - {from: "scripts/detect-stack.sh", to: "config/stack-mappings.json", via: "jq consumption of the merged mobile object"}
---
Scope amendment: finding 2's identity contract made the manifest-less fixture invalid. Commit 37edcbc6 added its manifest through the shared `make_root` factory.
<objective>
Fix the five verified findings from the Phase 05 adversarial review of phases 1-4: resolver candidate masking, unrelated-plugin root selection, space-in-path recovery, duplicate mobile stack mappings, and non-portable version sort in SessionStart cache cleanup. Root-cause fixes only, each with mechanical test coverage.
</objective>
<context>
@.vbw-planning/phases/05-adversarial-review-and-fix/05-RESEARCH.md
@scripts/resolve-plugin-root.sh
@tests/resolve-plugin-root.bats

Findings covered: RESEARCH findings 1 through 5 (all). Source precedence documented in CLAUDE.md "Command cascade" must remain unchanged. Fixes select the first valid candidate WITHIN each source, they do not reorder sources. VBW identity lives at .claude-plugin/plugin.json with name "vbw". The portable version-sort pattern already exists at scripts/session-start.sh:57-58. Reuse it rather than inventing a new one.
</context>
<tasks>
<task type="auto" correctness="dijkstra">
  <name>Resolver: pick first valid cache candidate instead of only the newest</name>
  <files>
    scripts/resolve-plugin-root.sh
    tests/resolve-plugin-root.bats
  </files>
  <action>
Finding 1. Both the numeric cache branch (lines 92-96) and the generic cache branch (lines 108-112) select the max candidate and validate once.
Replace that with an ordered scan.
Sort candidates version-descending, validate each with valid_root in that order, and stop at the first valid root.
Loop invariant: every candidate ranked above the current one has already been validated and rejected.
Do not change source precedence or the valid_root sentinel logic.
  </action>
  <verify>
Add three BATS cases. (a) Numeric branch: an invalid newest entry (missing scripts/hook-wrapper.sh) plus a valid older entry resolves to the older entry. (b) Generic branch: an invalid lexically-last entry plus a valid earlier entry resolves to the valid one. (c) Invalid-newest with NO lower valid fallback and no other sources exits 1. Run bats tests/resolve-plugin-root.bats.
  </verify>
  <done>
All new and existing resolver BATS cases pass. Commit: fix(scripts): resolve first valid plugin-root cache candidate
  </done>
</task>
<task type="auto">
  <name>Resolver: require VBW identity for marketplace and process fallback roots</name>
  <files>
    scripts/resolve-plugin-root.sh
    tests/resolve-plugin-root.bats
  </files>
  <action>
Finding 2. Add an identity check to the marketplace fallback (lines 118-123) and the ps-derived --plugin-dir fallback (lines 141-145).
A candidate is accepted only when it contains .claude-plugin/plugin.json and jq -r '.name' returns "vbw".
Use jq per repo convention (never grep on JSON).
Leave explicit CLAUDE_PLUGIN_ROOT and cache-source acceptance criteria unchanged.
Their trust model was reviewed and does not require identity validation.
  </action>
  <verify>
Add BATS collision cases. A valid-looking non-VBW marketplace candidate (has hook-wrapper.sh and commands/vibe.md but plugin.json name != vbw, or no manifest) sorted before a real VBW candidate must be skipped in favor of the VBW one. Repeat the same shape for the process fallback with mocked ps output. Run bats tests/resolve-plugin-root.bats.
  </verify>
  <done>
Collision cases pass, resolver never repairs the session link to a non-VBW directory in tests. Commit: fix(scripts): validate VBW identity for fallback plugin roots
  </done>
</task>
<task type="auto" correctness="dijkstra">
  <name>Resolver: recover --plugin-dir paths containing spaces</name>
  <files>
    scripts/resolve-plugin-root.sh
    tests/resolve-plugin-root.bats
  </files>
  <action>
Finding 3. Replace the whitespace-delimited extraction grep -oE -- "--plugin-dir [^ ]+" (lines 141-142).
The new parsing must handle a quoted, space-containing path after --plugin-dir in single-quoted, double-quoted, and unquoted-no-space forms.
Strip surrounding quotes before passing to valid_root.
If full shell-quote parsing is disproportionate, the minimum acceptable form handles the quoted-path cases produced by real claude invocations.
Escaped-space-without-quotes may be excluded only if documented in a comment naming the ceiling.
  </action>
  <verify>
Add a mocked-ps BATS case where the only source is a --plugin-dir install under a path with a space (e.g. "/tmp/Claude Plugins/vbw") and the resolver returns that exact path. Run bats tests/resolve-plugin-root.bats and shellcheck -S warning scripts/resolve-plugin-root.sh.
  </verify>
  <done>
Quoted-space case passes with no shellcheck regressions.
Commit: fix(scripts): parse quoted --plugin-dir paths in process fallback
  </done>
</task>
<task type="auto">
  <name>Merge duplicate mobile stack-mapping category</name>
  <files>
    config/stack-mappings.json
    scripts/detect-stack.sh
  </files>
  <action>
Finding 4. config/stack-mappings.json defines top-level "mobile" twice (lines 237-263 and 281-299).
jq keeps only the later one and silently drops android-kotlin, flutter, and react-native.
Merge all five profiles (android-kotlin, flutter, react-native, ios-swift, swiftui) into a single "mobile" object.
Preserve the current Swift profile content.
Add a duplicate-key integrity check to the existing test/verify layer (a jq comparison of raw key count vs unique key count, or equivalent) so a future duplicate top-level category fails CI.
Only touch scripts/detect-stack.sh if the merged object exposes a consumption bug, no behavior redesign.
  </action>
  <verify>
jq -c '.mobile | keys' config/stack-mappings.json returns all five profile keys. Detector fixtures assert Android (build.gradle.kts + Kotlin), Flutter (pubspec.yaml), and React Native (react-native in package.json) projects each emit their mobile profile via scripts/detect-stack.sh. Integrity check fails when a duplicate top-level key is introduced in a fixture copy.
  </verify>
  <done>
All five mobile profiles reachable, detector tests green, integrity check in place.
Commit: fix(config): merge duplicate mobile stack-mapping category
  </done>
</task>
<task type="auto">
  <name>Portable version sort in SessionStart cache cleanup</name>
  <files>
    scripts/session-start.sh
  </files>
  <action>
Finding 5. scripts/session-start.sh:569 assigns VERSIONS=$(ls ... | sort -V) with no BSD fallback, so stock macOS sort yields an empty list and cache cleanup silently no-ops. Apply the same portable pattern already used at lines 57-58 (sort -V 2>/dev/null || portable numeric fallback), or extract that existing two-line pattern into a shared function within the script if both call sites can use it verbatim. No new helper script.
  </action>
  <verify>
Add a BATS case that shadows sort with a stub rejecting -V (exit 2 on -V, else delegate) and asserts cache cleanup still orders versions correctly and retains only the latest cache version. Run bash -n scripts/session-start.sh and shellcheck -S warning scripts/session-start.sh.
  </verify>
  <done>
Cleanup works under the no-V sort stub, existing session-start tests still pass. Commit: fix(scripts): portable version sort in session-start cache cleanup
  </done>
</task>
</tasks>
<verification>
1. bats tests/resolve-plugin-root.bats passes including the new invalid-newest, collision, and quoted-path cases.
2. jq -c '.mobile | keys' config/stack-mappings.json lists exactly ["android-kotlin","flutter","ios-swift","react-native","swiftui"] (order per jq key sort).
3. bash testing/verify-shared-contracts.sh exits 0 (Phase 04 dedup untouched).
4. bash testing/verify-plugin-root-resolution.sh exits 0 (command cascade contract intact).
5. bash testing/run-all.sh passes clean (zero-tolerance policy).
</verification>
<success_criteria>
- All five RESEARCH findings fixed at root cause with a regression test each.
- Resolver source precedence order unchanged, only within-source candidate selection and fallback identity validation changed.
- Five atomic commits, one per task, {type}({scope}) format.
</success_criteria>
<output>
05-01-SUMMARY.md
</output>
