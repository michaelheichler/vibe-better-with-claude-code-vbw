---
phase: 6
plan: 03
title: qa-result-gate.sh four-library split and rtk-manager.sh environment-helper extraction
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: [repo-hygiene:keeping-files-small, gitnexus-refactoring, gitnexus-impact-analysis]
files_modified: [scripts/qa-result-gate.sh, scripts/rtk-manager.sh, tests/qa-result-gate.bats, testing/verify-rtk-integration-contract.sh]
files_touched: [scripts/qa-result-gate.sh, scripts/rtk-manager.sh, scripts/lib/qa-result-gate-path-evidence.sh, scripts/lib/qa-result-gate-fail-classifications.sh, scripts/lib/qa-result-gate-known-issues.sh, scripts/lib/qa-result-gate-summary-deviations.sh, scripts/lib/rtk-manager-environment.sh, tests/qa-result-gate.bats, testing/verify-rtk-integration-contract.sh]
forbidden_commands: []
must_haves:
  truths:
    - "Behavior preserved: only pure helper functions move. No release, smoke, installation, or hook workflow moves out of rtk-manager.sh, and no dispatcher or aggregation logic moves out of qa-result-gate.sh"
    - "qa-result-gate.sh keeps its SCRIPT_DIR assignment via $0 at line 11. Switching to BASH_SOURCE would widen the behavior-preservation diff (06-RESEARCH.md Finding 7)"
    - "The non-contiguous late-helper island at qa-result-gate.sh:1441-1650 moves with the other helpers, not left among the dispatcher"
    - "The known pre-existing rtk-manager smoke failure (tests/rtk-manager.bats:2058-2069, multiple KNOWN-ISSUE refs) is reported as pre_existing, not fixed or masked in this plan"
    - "No function moved is executed at source time. Sourcing order matches the existing scripts/lib precedent"
  artifacts:
    - {path: "scripts/lib/qa-result-gate-path-evidence.sh", provides: "34 path, canonicalization, and git evidence helpers", contains: "recorded_paths_are_fully_corroborated"}
    - {path: "scripts/lib/qa-result-gate-fail-classifications.sh", provides: "20 FAIL classification helpers", contains: "classification_ids_cover_source_fail_ids"}
    - {path: "scripts/lib/qa-result-gate-known-issues.sh", provides: "7 known-issue JSON helpers", contains: "load_known_issue_registry_json"}
    - {path: "scripts/lib/qa-result-gate-summary-deviations.sh", provides: "13 summary deviation helpers", contains: "summary_deviation_is_accepted"}
    - {path: "scripts/lib/rtk-manager-environment.sh", provides: "31 environment, path, version, receipt, settings, checksum, platform helpers", contains: "os_arch_target"}
  key_links:
    - {from: "scripts/qa-result-gate.sh", to: "scripts/lib/qa-result-gate-*.sh", via: "source after initialization at lines 35-47"}
    - {from: "scripts/rtk-manager.sh", to: "scripts/lib/rtk-manager-environment.sh", via: "source after globals at lines 15-29"}
    - {from: "tests/qa-result-gate.bats:4595-4622", to: "scripts/lib/qa-result-gate-known-issues.sh", via: "updated structural span assertion target"}
---
<objective>
Extract the pure helper layers of scripts/qa-result-gate.sh (2,158 lines, 74 helpers into four libraries) and scripts/rtk-manager.sh (2,096 lines, 31 environment helpers into one library, scoped per 06-CONTEXT.md) into sourced scripts/lib files, preserving behavior exactly. Independent of the markdown plan and the phase-detect plan: fully disjoint file sets.
</objective>
<context>
Context and research artifacts were not persisted. Rely on tracked planning records and shipped files.
@/Users/michael/.claude/plugins/cache/repo-hygiene-dev/repo-hygiene/0.1.0/skills/keeping-files-small/SKILL.md
@/Users/michael/.claude/skills/gitnexus-refactoring/SKILL.md
@/Users/michael/.claude/skills/gitnexus-impact-analysis/SKILL.md

Rationale: 06-RESEARCH.md Findings 7-9 verified that all 57 qa-result-gate helpers are pure at source time, that BATS suites invoke both scripts as subprocesses and copy the full scripts dir (so scripts/lib travels with them), and that exactly one test inspects source spans directly (tests/qa-result-gate.bats:4595-4622). The GitNexus index is stale for these files, so the Finding 7-9 static tables are the impact authority. rtk-manager extraction is deliberately scoped: release, smoke, install, and hook workflows have side effects and stay put. No forced line-count target (phase success criteria).

CONCERNS coverage: resolves milestone concern C4 for both scripts. STATE.md records an open qa-result-gate metadata-only gap (ref:2a7c918e). That is out of scope here: this plan moves helpers, it does not change gate policy.
</context>
<tasks>
<task type="auto">
  <name>Extract qa-result-gate path-evidence and fail-classification libraries</name>
  <files>
    scripts/qa-result-gate.sh
    scripts/lib/qa-result-gate-path-evidence.sh
    scripts/lib/qa-result-gate-fail-classifications.sh
  </files>
  <action>
Move the 34 path-evidence helpers from the Finding 7 table into scripts/lib/qa-result-gate-path-evidence.sh. Move the 20 fail-classification helpers into scripts/lib/qa-result-gate-fail-classifications.sh. Source both after initialization. Keep the existing $0-based SCRIPT_DIR assignment at line 11 unchanged. Move function bodies verbatim. Ref: C4.
  </action>
  <verify>
`bash -n` and `shellcheck -S warning` on all three files. `bats tests/qa-result-gate.bats` green (the pre-existing rtk-manager smoke failure lives in a different suite).
  </verify>
  <done>
Two libraries exist with verbatim function bodies, main script sources them, full qa-result-gate.bats green.
  </done>
</task>
<task type="auto">
  <name>Extract qa-result-gate known-issues and summary-deviations libraries, update span test</name>
  <files>
    scripts/qa-result-gate.sh
    scripts/lib/qa-result-gate-known-issues.sh
    scripts/lib/qa-result-gate-summary-deviations.sh
    tests/qa-result-gate.bats
  </files>
  <action>
Move the 7 known-issue JSON helpers into scripts/lib/qa-result-gate-known-issues.sh. Move the 13 summary-deviation helpers from the late helper island into scripts/lib/qa-result-gate-summary-deviations.sh, per the Finding 7 table.

Keep the island out of the dispatcher. Retain argument handling, target and scope selection, verification exits, aggregation, diagnostics, and routing in the main file. Update the structural span test to read scripts/lib/qa-result-gate-known-issues.sh. Ref: C4, 06-RESEARCH.md Finding 9.
  </action>
  <verify>
`bash -n` and `shellcheck -S warning` on all changed files. `bats tests/qa-result-gate.bats` fully green including the updated span test.
  </verify>
  <done>
Four libraries total, dispatcher-only main file for helper concerns, span test targets the new library and passes.
  </done>
</task>
<task type="auto">
  <name>Extract rtk-manager environment-helper library (scoped)</name>
  <files>
    scripts/rtk-manager.sh
    scripts/lib/rtk-manager-environment.sh
  </files>
  <action>
Move exactly the 31 helpers listed in 06-RESEARCH.md Finding 8 (now_utc through os_arch_target, lines 56-385) into scripts/lib/rtk-manager-environment.sh, sourced after the globals at rtk-manager.sh:15-29. Keep cleanup_temp_dir, usage, die, and every release, smoke, and dispatch workflow in the main file. Do not extract further to chase a line count. Ref: C4, 06-CONTEXT.md scoped-extraction decision.
  </action>
  <verify>
`bash -n` and `shellcheck -S warning` on both files. `bats tests/rtk-manager.bats`, expecting green except the documented pre-existing smoke failure at 2058-2069 (report it as pre_existing with test, file, error).
  </verify>
  <done>
Environment library exists with the exact 31 helpers, main script sources it, rtk-manager.bats matches its pre-split pass/fail profile.
  </done>
</task>
</tasks>
<verification>
1. `bats tests/qa-result-gate.bats` fully green.
2. `bats tests/rtk-manager.bats` matches its pre-split profile (known smoke failure only, reported as pre_existing).
3. `bash -n` and `shellcheck -S warning` clean on both main scripts and all five new libraries.
4. `git diff` of moved function bodies against their original ranges shows verbatim moves only.
5. `bash testing/run-all.sh` green (modulo documented pre-existing failures) before the final commit.
</verification>
<success_criteria>
- qa-result-gate.sh is a dispatcher sourcing four helper libraries with unchanged gate behavior.
- rtk-manager.sh sources one environment library containing exactly the 31 scoped pure helpers.
- Contract, lint, and bats suites stay green after each split, with the pre-existing rtk smoke failure reported, not masked.
</success_criteria>
<output>
06-03-SUMMARY.md
</output>

## Amendments (R01)

- **deviation-06-03-rtk-contract-rewrite** (resolved-by-amendment): Seven raw-main-script assertions in `testing/verify-rtk-integration-contract.sh` broke by construction when the 31 environment helpers moved into `scripts/lib/rtk-manager-environment.sh`. The script now scans effective content across the main script and the sourced library. Commit `bfe4c9d0` delivered the rewrite. QA verified 70 PASS and 0 FAIL.
- **deviation-06-03-smoke-not-reproduced** (resolved-by-amendment): The must_have truth expecting the documented smoke failure at `tests/rtk-manager.bats:2058-2069` to be reported as `pre_existing` rested on a stale premise. The failure did not reproduce in either the baseline or post-split run. Both runs passed 104/104 tests, independently confirmed by QA. `pre_existing_issues` was correctly left empty, and nothing was masked.

The original task bodies and must_haves remain unchanged.
